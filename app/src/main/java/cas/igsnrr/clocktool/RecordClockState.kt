package com.example.clockmachine.mainActivity.com.example.clockmachine.mainActivity

object RecordClockState {
    var clockState = 1   //2为签到，3为签退，1为进入签到
    var restartSate = 0  //0为不开启更新。1为开启
    var mapClockState = 0 //1为签到状态，2为签退状态，3为更新
    var test2 = ""
}